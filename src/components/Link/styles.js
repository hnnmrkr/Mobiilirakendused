import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginTop: 30
    },
    title: {
        color: '#4F63AC',
        textAlign: 'center',
        fontSize: 16,
        fontWeight: 'bold'
    }
})